============\\\\\How to build zenfone kernel/////============

Sync Up this repo.
Set up uber-4.9 toolchain for aarch64/arm64.
Open Terminal in kernel folder.
Add the following to .bashrc
<coming soon>
run make ze550kl_defconfig
run make -jx where x depends on the number of CPU cores you have.
Now you have your kernel (named as Image) in /arch/arm64/boot.
Flash it to your phone.

	
